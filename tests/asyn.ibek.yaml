module: asyn
entities:
  - name: AsynIP
    args:
      - type: str
        name: name
        description: Asyn port name

      - type: str
        name: port
        description: Asyn port number

    script: PMACAsynIPPort({{name}}, {{port}})
