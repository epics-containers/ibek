# yaml-language-server: $schema=../schemas/ibek.support.schema.json

module: documented

entity_models:
  - name: documented
    description: a test device that ships documentation
    parameters:
      name:
        type: id
        description: identifier
      P:
        type: str
        description: pv prefix
