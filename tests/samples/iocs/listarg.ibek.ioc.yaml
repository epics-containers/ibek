# yaml-language-server: $schema=../schemas/listarg.ibek.schema.json
ioc_name: counter
description: an example motion ioc for ibek testing

entities:
  - type: listtest.lister
    # test an actual list of strings
    friends: ["Psiren", "Vindaloovian", "Polymorph"]

  - type: listtest.lister
    # test a jinja template that renders to a list
    friends: |-
      {{ (["Rimmer"] + ["Parallel Holly"]) * 2  }}
